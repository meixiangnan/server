#include "rt_base_config.h"


const std::string game::RtBaseConfig::configPath = "../config/rt_base_config.json";

void game::RtBaseConfig::Init()
{
    //drogon::ConfigLoader loader(game::RtBaseConfig::configPath);
    //loader.load();
    //auto jsonValue = loader.jsonValue();
    //this->ParseFrom(jsonValue);
}
